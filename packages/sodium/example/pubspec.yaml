name: sodium_example
description: Sample dart program for web and native

environment:
  sdk: ^3.2.0

dependencies:
  sodium:
    path: ..

dev_dependencies:
  build_runner:
  build_web_compilers:
  lint:

executables:
  native: main_native
