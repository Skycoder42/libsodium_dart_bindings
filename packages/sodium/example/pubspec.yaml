name: sodium_example
description: Sample dart program for web and native

environment:
  sdk: ^3.9.0
resolution: workspace

dependencies:
  sodium: ^3.4.6

dev_dependencies:
  build_runner: ^2.7.1
  build_web_compilers: ^4.2.4
  lint: ^2.8.0

executables:
  native: main_native
